---
description: 优先选用成熟、高 star、易维护的第三方库，避免重复造轮子
alwaysApply: true
---

# 优先使用成熟第三方库

实现功能时，**能由成熟第三方库承担的，不要从零手写**。选型偏向 **社区主流、GitHub star 较高、近期仍活跃维护、文档完善** 的 SDK/库。

## 核心原则

1. **先查现有栈**：`package.json` 与 `.cursor/rules` 已约定的库优先复用
2. **再搜成熟方案**：防抖、缓存、路由、表单、图表、日期等通用能力，优先选大众库
3. **最后才自研**：仅当无合适库、或与本 ERP 强耦合、或引入成本明显高于自写时，才在项目内实现

## 本项目已选型的库（优先复用）

| 场景 | 库 |
|------|-----|
| UI 组件 | `antd`、`@ant-design/icons` |
| 服务端异步数据 | `@tanstack/react-query` |
| 跨页会话状态 | `zustand` |
| 交互 hook（防抖、稳定回调、弹窗） | `ahooks` |
| 路由 | `react-router-dom` |
| 日期时间 | `dayjs`（入口 `@/lib/dayjs`） |
| 构建 | `vite`、`less` |

新增能力时，先判断能否用上表或同生态扩展（如 antd 表单、React Query mutation），**不要平行再引入同类库**（例如已有 dayjs 不要再加 moment）。

## 新依赖选型标准

引入**新** npm 包前，快速核对：

| 维度 | 建议 |
|------|------|
| 流行度 | npm 周下载可观；GitHub **star 较高**（同类中靠前） |
| 维护 | 近 6–12 个月有 release 或 commit；issue 有响应 |
| 体积与依赖 | 依赖树不过度膨胀 |
| 兼容性 | 支持现代浏览器与 Vite；无多余 Node native 绑定进渲染层 |
| 许可证 | MIT / Apache-2.0 等可与项目兼容 |
| TypeScript | 自带类型或 `@types/*` 完善 |

**倾向**：React 生态主流（TanStack、Ant Design 系）、久经生产验证的库。

**谨慎**：个人小众库、长期无更新、star 极低、文档缺失、强绑特定云厂商的 SDK（除非业务明确要求）。

## 应当用库的典型场景

- 防抖 / 节流 / 轮询 / 生命周期 → `ahooks`
- 列表拉取、缓存、重试、失效刷新 → `react-query`
- 复杂表单校验 → antd `Form` 规则
- 日期解析格式化 → `dayjs`
- 路由与导航守卫 → `react-router-dom`

## 适合自研、不强行上库的场景

- 与本 ERP **强耦合的业务规则**（单据状态机、权限码）
- 一行能写清、且无第二处复用点的逻辑（勿为单行抽库）

## 引入新依赖的流程

1. 说明要解决什么问题，列出 1–2 个候选库及 star/维护情况
2. `pnpm add` 后跑 `pnpm run typecheck`
3. 不重复引入与现有栈功能重叠的依赖

## 禁止

- 手写已有成熟库覆盖的能力（如自写 debounce、自写全局请求缓存）
- 为「少一个依赖」复制粘贴大段第三方源码进仓库
- 引入 star 极低、多年未维护、无替代理由的库
- 未经评估向 `package.json` 堆叠多个同类库

## 与其他规则的关系

数据与交互分层仍以 `data-layer.mdc` 为准；本规则解决 **「用什么库」**，不推翻分层约定。
